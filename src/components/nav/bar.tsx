import Button from "./Button"

export default function () {
    render() {
        return (
            <nav>
                <Button />
                <Button />
                <Button />
            </nav>
        )
    }
}