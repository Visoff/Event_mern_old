import Button from "./Button"

import "./bar.css"

export default function () {
    return (
        <nav>
            <Button href="home" name="Главная" />
            <Button />
            <Button />
        </nav>
    )
}